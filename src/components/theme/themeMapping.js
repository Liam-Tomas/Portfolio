import { lightTheme, darkTheme, redLightTheme, redDarkTheme } from './CustomColors'

const themeMapping = {
    light: lightTheme,
    dark: darkTheme,
    redLight: redLightTheme,
    redDark: redDarkTheme
};

export default themeMapping;
